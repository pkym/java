package 일차원배열;

import java.util.Scanner;

public class ex07배열복습 {

	public static void main(String[] args) {
		// 배열이란...?
		// 동일한 자료형(int, string등)이 순차적으로 나열된 것
		// 각 공간안에 번호를 매겨놨다. 그것을 우리는 인덱스라 부른다
		// 배열의 인덱스는 0부터 시작한다.
		// 배열은 값을 담는 변수가 아닌 주소를 담는 레퍼런스 변수이다
		// 배열의 길이는 배열 생성 시 결정, 나중에 바꿀 수 없다.
		// 배열의 길이는 length라는 필드에 저장,나중에 가져다 쓸 수 있음

		// 생성방법
		// 1. 배열을 먼저 생성하고 나서 값을 넣는 방법
		// 예시) int[] array=new int[3];
		// array[0]=10; array[1]=20, array[2]=30;

		// 2. 배열 생성과 동시에 값을 넣는 방법
		// 예시) int[] array={10, 20, 30};

		
		

	}

}
